ktor {
    deployment {
        port = 8080
        port = ${?PORT}

        connectionGroupSize = 2
        workerGroupSize = 5
        callGroupSize = 10
    }
    application {
        modules = [ com.fadinglight.ApplicationKt.module ]
    }
}

database {
    url = "mongo://localhost"
    username = "clz"
    password = "123456"
    database = "bill"
}